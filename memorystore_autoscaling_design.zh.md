# 托管缓存服务的原生自动扩缩容:设计提案

> **来源说明。** 本文综合自公开文档与开源的 Memorystore Cluster Autoscaler。这是一份
> 设计提案,不代表任何厂商的路线图、内部架构或定价。

另见 [memorystore_serverless_design.md](memorystore_serverless_design.md):本文提出的
控制器是 serverless 缓存所需三层中的第三层,那篇文档覆盖其下的两层。

## 1. 问题

托管缓存按容量交付:运维者手动选定节点规格与分片数,并手动调整。缓存负载很少稳定——
昼夜潮汐、突发事件——而其成本是不对称的。**过度配置**的代价是持续付费;**配置不足**的
代价则会一次性到来,以驱逐(eviction)或 OOM 的形式爆发。

Memorystore 没有原生自动扩缩容。想要该能力的客户会部署
[Memorystore Cluster Autoscaler](https://github.com/GoogleCloudPlatform/memorystore-cluster-autoscaler)——
一个由他们自行托管和运维的开源工具。本文提出用**服务端自动扩缩容**取代它。

## 2. 现状

**AWS ElastiCache** 提供两种模型。*Serverless* 用一层代理集群隐藏拓扑,连续地扩缩计算
(ECPU)、内存与网络;运维者以最大存储和 ECPU 上限来约束成本。*基于节点的集群*则使用
Application Auto Scaling,以 CloudWatch 指标做目标追踪(target-tracking)。客户端直连
节点,因此一次 reshard 对客户端可见,而缩容(scale-in)是被层层设防的:目标值以下 25%
的死区(deadband)、600 秒冷却、缺数据时不缩容、任一策略同意即扩容而所有策略同意才缩容,
以及两条硬性拒绝——当某个 slot 持有序列化后超过 256 MB 的条目时不移除分片,或当缩容后的
配置内存不足时不移除分片。AWS 建议初始就**禁用缩容**。

**Azure** 不做自动扩缩容,并
[明确不推荐](https://learn.microsoft.com/en-us/azure/azure-cache-for-redis/cache-best-practices-scale),
理由是扩缩操作本身会消耗缓存资源。这看起来像是对*反应式*扩缩的反对,但证据另有指向:
Azure Managed Redis 运行在 Redis Enterprise 之上,其每节点代理早已隐藏了拓扑——正是
ElastiCache Serverless 花代价去做的解耦——而其
[架构页](https://learn.microsoft.com/en-us/azure/redis/architecture)
仍然写着*"目前不支持缩容(Scaling down isn't currently supported)"*。手动不行,按计划
也不行。所以这条反对针对的是缩容的**数据搬迁本身**,而非其时机。AWS 从另一端得出相同
结论:它交付了缩容,却又对它重重设防。

两家已经解决了拓扑问题的厂商,都拒绝把"收缩"当作例行操作。**缩容安全是本设计的核心
(§5),而非附录。**

现有的 Memorystore Cluster Autoscaler 是一个 poller → scaler → API 的控制回路。它的
**形态是对的**;它的**局限是运维性的**——客户自行托管它、为其算力付费、并维护其
Terraform。

## 3. 目标

- **全托管。** 无需客户部署任何基础设施。
- **零停机。** 不断连接、不丢数据。驱动 Memorystore 已有的零停机扩缩原语,而不是新增一个。
- **目标追踪。** 用利用率指标(内存、CPU)配一个目标值,而非阈值阶梯。
- **有界。** 运维者定义的最小与最大容量。
- **有阻尼。** 每次事件后设冷却期,防止震荡。
- **缩容安全(§5)。**

## 4. 架构

把 poller/scaler 回路移入服务控制平面;把策略表达为实例上的一个资源。

```
  ┌──────────────────────────────────────────────────┐
  │  API 层(无状态,请求驱动)                       │
  │   • 校验并持久化策略                              │
  │   • 不运行任何后台回路                            │
  └───────────────────────┬──────────────────────────┘
                          │ 策略
                          ▼
  ┌──────────────────────────────────────────────────┐
  │  控制器(后台,有状态)                           │
  │   • 采样利用率指标                                │
  │   • 评估目标追踪策略                              │
  │   • 施加边界、冷却、安全检查                      │
  │   • 驱动已有的扩缩操作                            │
  └───────────────────────┬──────────────────────────┘
                          ▼
                    数据面扩缩
```

API 层是无状态的,所以控制回路不能住在那里。控制器拥有回路、冷却状态,以及"是否行动"的
决策。策略是实例上的一个声明式对象——目标指标、目标值、最小/最大容量、冷却。具体的 API
形态属于 API 评审的范畴。

### 4.1 触发条件

给定目标 `T` 与死区 `d`(AWS 取 `d = 0.25`),控制器将每个利用率信号 `u` 与两条阈值比较:

| 方向 | 条件 | 持续窗口 |
|---|---|---|
| **扩容** | `u ≥ T` | 短(约 1–3 分钟) |
| **保持** | `T·(1−d) < u < T` | — |
| **缩容** | `u ≤ T·(1−d)` *且* §6.2 所有闸门通过 | 长(约 15–30 分钟) |

取 `T = 0.70, d = 0.25`,扩容在 70% 触发,缩容只在低于 52.5% 才触发。两条阈值之间的
间隙正是防震荡的关键;它不是可选项。这种不对称同时体现在三处——阈值(死区)、持续窗口
(扩容按分钟响应,缩容等待数十分钟)、冷却(缩容冷却 ≫ 扩容冷却)。

**两个方向监视不同的信号。**

- **扩容**看几个信号中*最糟*的那个(逻辑或——任一触发即可):
  - `used_memory_rss / maxmemory ≥ T_mem`——最紧迫的一个;扩容迟了就是驱逐或 OOM。
  - 主节点引擎 CPU `≥ T_cpu`——单线程已饱和。
  - 上升的 `evicted_keys` 或被拒连接——*紧急*信号,绕过持续窗口。
- **缩容**只有当*所有*信号都同意安全时才触发(逻辑与):内存与 CPU 都 `≤ T·(1−d)` 且
  持续,再加上 §6.2 的可行性与安全闸门。

这条"扩容取或 / 缩容取与"的规则,是 ElastiCache "任一策略同意即扩容,所有策略同意才缩容"
在多指标上的推广。

**用 RSS 触发,而非 `used_memory`。** 内存信号应取 `used_memory_rss / maxmemory`:RSS
才是内核据以杀进程、也是约束节点的量。仅以 `used_memory` 触发,会让碎片和缓冲把 RSS 推到
上限,而逻辑信号仍读数安全——一次"60% 利用率"下的 OOM。(§5 就缩容守卫展开了同样的
RSS-vs-`used_memory` 差距。)

**方向无关的封锁。** 当有扩缩操作、故障切换或维护正在进行时(让路,不排队)、相关冷却
尚未走完时、或缺数据时,两个方向都不触发——`INSUFFICIENT_DATA` 可以允许扩容,但绝不
允许缩容。

**扩容并非瞬时。** 基于 ASM 的扩容需要数分钟才能完成;内存可能填得更快。上面的紧急信号
能绕过持续窗口,却绕不过迁移本身,所以高速填充的负载可能在新容量上线前就已驱逐或 OOM。
这是*反应式*内存扩容所能承诺的下限,也是为何要有一层代理前置的 serverless 层——即本文
之下的那一层——去吸收节点集群无法吸收的瞬时冲击。控制器只能缩小这个差距,无法消除它:
尽早触发扩容(用比 CPU 目标更低的 `T_mem`),并靠最小容量下限与 survivor 余量(§6.6)
在新容量落地期间顶住突发。

**触顶之后已无计可施。** `max` 约束成本(§3),但在 `max` 上持续攀升的负载,会到达
控制器本要防止的那个失败——先驱逐、后 OOM——而已无任何扩缩动作可用。控制器不能假装无事:
到 `max` 就停止扩缩并升级上报(告警、暴露饱和状态),而不是默默承受这份超额。因此为 `max`
定值是一个容量决策,而不仅是成本上限——应按最坏的预测突发来设,而非按预算。这道天花板是
反应式模型的边界;serverless 层的连续扩缩才是对它的结构性回答。

## 5. 安全

扩容费的是钱,缩容费的是数据。**当幸存节点无法容纳工作集时,必须拒绝缩容**——而那个显而
易见的检查,即拿目标容量与 `used_memory` 相比,是错的:

> `used_memory` 报告的是活跃分配量。内核据以杀进程的是 RSS。两者之间坐着:分配器碎片、
> 复制缓冲、AOF 缓冲、客户端 I/O 缓冲,以及缩容自身 fork 出的快照所产生的写时复制(CoW)页。

以 `used_memory` 为基准去定容,一次缩容可能让某个节点落在其逻辑上限之内、却仍被 OOM 杀掉。
守卫必须以 **RSS 加瞬时开销**为基准来定容,其中包含迁移的写时复制成本。

引擎不会替我们兜住这一点。
[原子 slot 迁移](design-docs/atomic-slot-migration.md)会在目标端内存不足时回滚——但一个
OOM *错误*是一次 `maxmemory` 违规、一个 `used_memory` 事件,而内核据以杀进程的是 RSS。
如果 RSS 在 `used_memory` 越过 `maxmemory` 之前先越过了容器上限,就没有回滚,只有一个死掉
的主节点和一次故障切换。

所以守卫是两道守卫:

- **准入预检**(控制器):除非缩容后的配置能容纳 RSS 加瞬时开销,否则拒绝缩容。
  ElastiCache 也是这么做的,当"缩容后的分片配置可用内存不足"时拒绝移除分片。
- **在途回滚**(引擎):ASM 已有的触发器,经过校准使 `maxmemory` 守卫在 OOM killer 之前先
  触发。

Azure 公布了一个校准值:约 **20%** 的内存被保留"用于非缓存操作,例如故障切换期间的复制"。
20% 是否恰当尚可争论;但一个厂商为这些项保留五分之一的内存,已是这道差距真实存在的有力证据。

另有两条约束:

- **每次操作后都设冷却**,其长度要超过该操作的持续时间。
- **不对称的激进度。** 扩容要快、缩容要慢:扩容迟了费的是延迟,缩容急了费的是数据。

## 6. 缩容

§5 给出原则。本节给出机制,以及当前引擎中使自动缩容今天还不能安全上线的两个阻碍。

### 6.1 当前引擎中的两个阻碍

**引擎无法度量一个 slot 装了多少。** 准入检查要问"某个幸存者会从这段 slot 范围吸收多少
字节?",而引擎答不上来。`CLUSTER SLOT-STATS`
([src/cluster_slot_stats.h](src/cluster_slot_stats.h))暴露四个每-slot 指标——
`key-count`、`cpu-usec`、`network-bytes-in`、`network-bytes-out`——没有内存。唯一的
替代物 `key-count × 平均键大小`假设值大小均匀,而这恰在缩容最危险的倾斜负载上失效:一个
slot 里的一个大集合就能让它偏差几个数量级。这大概正是 ElastiCache 拒绝迁移持有超过 256 MB
条目的 slot 的原因——对一项 AWS 同样无法完成的度量,一个粗糙的替身。

**在典型缓存上,引擎的内存守卫根本触不到。** 在导入目标端,slot 迁移客户端上的一次 OOM
会走到 [src/server.c:4564](src/server.c) → `clusterHandleSlotMigrationClientOOM()`,由其
回滚。但那条分支只在 `performEvictions()` 返回 `EVICT_FAIL` 时才执行,而
[src/evict.c:422](src/evict.c) 只在 `maxmemory-policy noeviction`(或 `import-mode`)下才
返回 `EVICT_FAIL`。在 `allkeys-lru` 或任何其他驱逐策略下,它会驱逐并返回 `EVICT_OK`。

所以在一台正常配置的缓存上,目标端在自己没有空间时**不会回滚**。它会**驱逐自己的工作集
来给流入的 slot 腾地方,静默地,并报告成功。** 缩容费的是数据——以驱逐、而非 OOM 的形式。
这正是 AWS 说明其容量指标"在 `maxmemory-policy` 设为 `noeviction` 时效果最好"的原因。而
即便在 `noeviction` 下,守卫触发于 `used_memory`,内核却杀于 RSS——就是 §5 那道差距。

### 6.2 何时:三道闸门,全部通过

- **资格(Eligibility)。** 利用率持续低于目标值并留有死区(AWS 取 25%),窗口长于一次迁移
  的耗时。缺数据不等于低利用率。距上一次操作(任一方向)的冷却已走完。
- **可行性(Feasibility)。** 无迁移、故障切换或维护在进行——让路,不排队。集群健康,每个
  幸存分片都有其副本。待清空集合中没有 slot 持有大到会溢出源端客户端输出缓冲的条目(那是
  一个 ASM 回滚触发器)。
- **安全性(Safety)。** 每个幸存者*及其副本*都能容纳迁移后的 RSS 加开销,并留有余量
  (约 20%)。按 §6.1,这在当前引擎上不可计算。

### 6.3 移除什么:先副本,后分片

缩容有两个维度,成本相差一个数量级:

- **副本(Replica)。** 一个副本离开不搬任何数据;它注销即可。近乎即时、可逆、低风险。
  代价是读容量和一份 HA 拷贝。
- **分片(Shard)。** 移除一个分片要把它的 slot 迁到幸存者——即 ASM 路径,带着 §5 和 §6.1
  的全部开销。

当压力落在副本所服务的东西上(副本 CPU、读吞吐)、而主/内存余量充足时,移除一个副本。
只有当聚合内存或主 CPU 余量足以让出一个分片的容量时,才移除一个分片。ElastiCache 正是
为此把这两者作为独立的可扩缩维度暴露出来。

**选哪个分片。** 移除单位是整个分片——主及其副本一起离开。顺序如下:

1. **可行性过滤。** 排除持有超大条目 slot 的分片,或其移除会跌破最小分片数、或破坏可用区
   分布的分片。
2. **数据最少。** 在其余分片中,挑持有字节最少的那个(有了内存指标就按内存;今天按
   `key-count`)。搬得最少、CoW 最少。
3. **模拟放置。** 把牺牲分片的 slot 铺到*所有*幸存者上,若任一幸存者或其副本会越过安全
   阈值或变成热分片,就否决该候选。
4. **均衡。** 优先选择使 slot 分布最均匀的那种移除。

牺牲分片的 slot 必须**铺开**,而不是倒给最近的那个幸存者——否则缩容是拿一个轻载集群换来
一个热分片,比不缩还糟。

### 6.4 怎么做:分批清空 slot

搬迁单位是一批 slot,而非一个分片:

1. 选定牺牲分片(§6.3)与目标 slot 分配。
2. 对每个幸存者及其副本跑一遍 §6.2 安全闸门。
3. 迁移 *k* 个 slot。重跑闸门。重复。
4. 当牺牲分片持有零个 slot 时,`CLUSTER FORGET` 掉它并释放节点。
5. 一旦回滚,进入冷却并上报。绝不立即重试。

分批把瞬时 CoW 与复制开销约束在数据集的约 `k/16384` 而非一整个分片的量级,让安全闸门可以
在清空途中被重新检查,并使回滚成本只是一批而非整个操作。原子式地清空一个分片,会把 §5 所
守卫的开销放到最大。

### 6.5 反应式扩容,计划式缩容

昼夜低谷是可预测的,而 AWS 指出计划式扩缩适合确定性负载、目标追踪适合其余的。在已知的
夜间低谷做一次缩容,不是在压力下执行的,这从操作上回应了 Azure 的反对(§2)。反应式缩容
才是两家厂商都不推荐的那件具体的事。这让 §5 的不对称成为一条操作规则,而非一个调参项。

### 6.6 默认值

- 缩容默认**关闭**。
- 死区至少低于目标值 25%。
- 缩容冷却大于实测的 p99 迁移耗时,并远长于扩容冷却。
- 幸存者上留 20% 内存余量。
- 自动缩容**除非内存守卫可达,否则拒绝运行**——即 `noeviction`,或让目标端为该任务进入
  `import-mode`。

最后这条默认值动用的是一个已有的原语。`import-mode`
([src/config.c:3392](src/config.c))在主节点上禁止驱逐与过期,并与 `noeviction` 一道强制
`EVICT_FAIL`([src/evict.c:422](src/evict.c))。在导入目标端为一个任务设上它,就能让 OOM
回滚即便在 `allkeys-lru` 缓存上也照样触发。代价:它同时暂停过期,因此一次长迁移会积累一批
待回收的过期键——这仍比静默驱逐工作集是更好的交易。

### 6.7 引擎前置条件

有两处引擎侧改动是安全自动缩容的前置。两者都小,且可向上游 Valkey 提交;在它们落地之前,
控制器应当*建议*缩容供运维者手动执行,而不是自己执行——这也正是 AWS 与 Azure 实际所处的
位置。

1. **给 `CLUSTER SLOT-STATS` 加一个 `memory-bytes` 指标。** 没有它,准入闸门(§6.2)就是
   猜测。
2. **让内存守卫在导入期间可达**——要求 `noeviction`,或让 ASM 为该任务在目标端设上
   `import-mode`。

## 7. 待解问题

- 哪个指标是主指标?内存是更安全的触发器,CPU 是更灵敏的;同时追踪两者需要一条冲突规则。
- 每-slot 的 `memory-bytes`(§6.7)是否便宜到能在热路径上保持精确,还是必须采样——从而重新
  引入 §6.1 的倾斜误差?
- 一次长时间的 `import-mode` 清空(§6.6)是否会积累足够多的过期未回收键,以致扭曲安全闸门
  所依赖的内存度量?
- 计划式缩容(§6.5)是否需要运维者声明的低谷,还是控制器应自行学习昼夜模式?
